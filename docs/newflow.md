 ich möchte mit dir nochmals über den eintragungsprozess sprechen. wie wir erfahrungen extrahieren. schritt eins soll der user erklärt bekommen was wichtig ist bei xp einträgen. eine
  vibrierende box signalisiert das eingeben der schatten virbiert in einem blau. im hintergrund ist es metallisch grau. die haupt farben sind neon blau , green, red, green.  di beim
  tippen erscheint anstatt des | cursors ein pasendes symbol animiert das zeigt wenn er tippt.  da wir eine mindestanzahl an buchstaben benötigen haben wir eine bar in farbverlauf welche
  sich füllt wenn er die bzw bis er die 50 wörter erreicht. maximal 9,999 wörter. wenn er mehr tipped. wird die bar größer von de rtimeline her und beim tippen füllt es sich wqeiter also
  50 wöter sind dann nicht bei m ende auf 1010% sondern es verschiebt sich in einer niamation nach links und die bar füllt sich bis wieder soll als motavation dienen einzutragen. in der
  mitte unten am fuße in der box steht ein modernes symbol für die sprach eingabe. wenn man mit der maus drüber fährt bekommt es auch so einen coolen hover effekt  und das symbol
  verändert sich. drüc hover text erklärt wozu der knopf ist. drückt man drauf schiebt sich das fenster vom button nach oben quasi über die text box . e  er scheint eine futuristische
  tron artivge wave diamit der user sieht , dass er spricht.  darüber steht noch kurz eine erklärung der beideung und auf was wichtig ist zu achten. dort wo der record button war
  verwandeln sich der eine knopf in weitere . pause, fertig.  knopf gibt es noch. sobald er fertig drückt. wir dder text in die textbox transkripiert dies passiert ials gesamter text der
  magisch modern in das bild faded. nicht klassischer typewriter animation. (der text wurde ja schon geschrieben sonst wartet der user ewig bis er "fertig" ist) ein tooltipp bzw moderne
  info box erscheint mit dem hinweiss, das dies nun erkannt wurde und der user bitte nochmal sicherheishalber drüberschuaen soll. rechts unten erscheint ein grüner button weiter.
  ebenfalls gibt es links buttons zum zurücksetzen und save draft (wir haben schon ein drat sytem implementiert enke denke ich)  der user drückt den grünen button und das fenster morved
  in einer smoothen animation in den nächsten step. über dem ganzen haben wir immer eine status anzeigen mit einem virbierenden leichtenhblauen status step % anzeige wie viele schritte es
  noch benötigt. die farben mttivieren von gelb bis > neon ). wenn der user auf weiter drückt. erscheint eine info in de rrichtung "folgende dinge wurden anhand deiner erfahrung
  automatisch erkannt". darunte rsieht er kompact einen Titel welcher automatisch mit openai generiert wurde. die kategorie (welche aus unserem backend abgeleitet und daruas erkannt
  wurde). die wichtigsten haupt und nebentags. haupt tags sind visuel  neon und fett. der user hat die möglichkeit mit X einzelne tags zu löschen. oder beim tippen eslbst welche
  hinzzufüpgen.  alle eingabefeler natürlich in unsrem grau mit neon farben. darunter erscheint eine box mit der erklärung, dass noch wichtige infos benötigt werden. hier kommen dann die
  zusatzfragen des backends. gleich hast du es geschafft, wir benötigen noch ein paar zusätzliche infos, damit wir besser ähnliche xps finden können.auch in dieser fragebox gibt es eine
  anzeige wie beim tippen mit der % bar. es sind maximal 3 seiten.  pro seite können maximal 2-3 fragen aufscheinen. wenn er weiter drückt kommt es zur nächsten frageseiten. nachdem die
  fragen auisgefüllt wurden (weiter ) erscheint eine meldung. xp share lebt von den oft irrelevant wirkenden details  - erzähle uns mehr und verdiene xp bonus punkte. (das ganze soll
  natüprlich besser geschrieben und formulisert werden). wenn der user weiter drückt kommt es zu weiteren fragen welche anhand der AI generierten dynamischen befragung passiert. der user
  kann zu jedem zeitpunkt naülrich vor und zurück spriungen in den fragen und  auf fertig drücken. danach geht es mit weiter in die nächste maske. finde du ein bessere betitelung. in der
  nächsten maske geht es darum das user bilder, videos, audios und via sketch whiteboard (haben wir shcon impementiert) anhänge hinzufügen können. jeden anhang kann man nach upload
  (welcher visuell dargstellt wir dbeim laden) eine eigene bbox für weitere infos zu den anhängen. ebenfalls kann man hier augenzeugen hinzufügen. mit info, dass erfahrunge an
  glaubwürdigkeit und energie gewinnen, wenn es mehrere menschen erlebt und bestätigen können. verweise wieder auf extra punkte hin. der user kann dann entweder jemand aus der plkattform
  suchen anhand des usernamens oder eine email generieren bzw einen invite link. dies alles bringt punkte, wenn equasi ein neuer user onboarded wurde und er dan nbestätigt (wir haben
  einen teil des invite systems bzw b witness system schon impelmentiert meine ich). auf der letzten seite finde einen passenden titel. hier sieht der user nochmal eine gesamt übersicht.
  es erscheint eine box mit dem geschriebenen text nicht die ganze grpoße box sondern nur die ersten paar zeilen mit expand button.  darüber der titel und darunter die tags (natürlich
  alles noch editierbar). bei der textbox ist per default AI enhanced aktiviert mit einem brain symbol. (hofer effekte etc wie imemr ) darüber steht ein infotext, dass der orignaltext mit
  den zusatzfragen angereichwer wurde um eine bessere lesbarkeit herzustellen (wie immer finde die passenden woirtre) . wenn der knopf deaktiviert ist dann erscheint das rohe orignal. je
  nachdem was der user wählt erscheint (schreibt das noch hinzu beim text) . . darunter gibt es ein eoption der privatsphären einstellung. privat (wird nicht veröffentlich), public,
  anonym (post wird veröffentlicht jedoch mit einem Anonym Userbadge quasi wir bestätigten das es den user gibt aber er möchte nicht erkannt werden. natürlich kann man später einem post
  welches anonym ist eine nachricht zukommen lassen (sofern der user dies vorher so eingestellt hat.) sobald der user dann auf XP Speichern drückt. landet man auf der seite sines
posts jedoch ist dies noch blurry weil im vordergrund eine smoothe lade animation mit einem ähnlcihe text: deine xP wurde gespeichert... wir suchen ähnlicheeinträge
  daraufhin erscheint ein neues fenster wo motivierend geschrieben wird wir haben ähnliche erfahrungen gefunden. eine auflistung der top xp posts inkl. infotext 
  mit konfidence score und warum dies ähnlich sein könnte. diese xps kann man öffnen. wenn er nicht will drückt er das X rechts oben und landet auf seiner post seite. 
  drückt man auf einen eintrag öffnet sie sich. man kann unten auch auf alle anzeigen gehen und man bekommt eine komplette liste (wo man natürlich in kacheln, liste etc sortieren undansicht erstellen kann)
  
  
  
  
  
  
  
  
  
  
  
  wir müssen ganz am ersten screen noch eine upload button einfüge. mit der info dazu, dass man auch hier OCR anweden kann
  damit man zb hangeschriebene dinge hier einscannen kann (natürlich mit deinen wörtern wieder). 