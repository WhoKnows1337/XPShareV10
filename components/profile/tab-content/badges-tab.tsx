'use client'

import { Card, CardContent } from '@/components/ui/card'

export function BadgesTab({ userId }: { userId: string }) {
  return (
    <Card>
      <CardContent className="py-12 text-center">
        <p className="text-muted-foreground">Badges tab content coming soon...</p>
      </CardContent>
    </Card>
  )
}
